# Переменные ТОЛЬКО для тестов. Отличие от .env — отдельная БД telemost_test,
# которую тесты вправе очищать под ноль.
PORT=4001
CLIENT_ORIGIN=http://localhost:5173
DB_HOST=localhost
DB_PORT=5432
DB_NAME=telemost_test
DB_USER=telemost
DB_PASSWORD=telemost

JWT_SECRET=test_secret_not_for_production
JWT_EXPIRES_IN=7d
